---
// Subtle background grid overlay
---

<div class="fixed inset-0 pointer-events-none z-0 opacity-[0.02]">
  <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
    <defs>
      <pattern id="background-grid" width="2" height="2" patternUnits="userSpaceOnUse">
        <path d="M 2 0 L 0 0 0 2" fill="none" stroke="currentColor" stroke-width="0.1" class="text-gray-900"/>
      </pattern>
    </defs>
    <rect width="100%" height="100%" fill="url(#background-grid)" />
  </svg>
</div>

<style>
  /* Ensure grid doesn't interfere with content */
  .fixed {
    pointer-events: none;
  }
</style>